name: Qualify D8 VPS runtime candidate

on:
  workflow_dispatch:
  pull_request:
    paths:
      - "AGENTS.md"
      - "contracts/d8-runtime-candidate.json"
      - "docs/semantics/d8-vps-unified-acquisition-runtime-v1.md"
      - "docs/semantics/d8-production-capability-parity-expansion-and-cutover-v1.md"
      - "docs/handoffs/d8-vps-runtime-integration-handoff-v1.md"
      - "tools/d8/Dockerfile"
      - "src/d8_runtime.py"
      - "src/d8_service.py"
      - "src/acquisition_core.py"
      - "src/d8_observation_normalizer.py"
      - "src/aife_server_adapter.py"
      - "src/aife_f5c_acquisition_adapter.py"
      - "tests/d8/**"
      - "AIFE/staging/server/acquisition/**"
      - "AIFE/staging/server/integration/bindings.py"
      - "AIFE/staging/server/runtime/composition.py"
      - "AIFE/staging/server/runtime/deployment.py"
      - "AIFE/staging/server/runtime/c9_forward.py"
      - "AIFE/staging/deploy/server/installer/aife_deploy.py"
      - "AIFE/staging/tests/integration/server/test_privileged_deployment_executor_integration.py"
      - "AIFE/staging/tests/unit/server/test_privileged_deployment_executor.py"
      - "AIFE/staging/tests/unit/server/test_bootstrap_package_renderer.py"
      - "AIFE/staging/tests/integration/server/test_f5_permissions_and_deployment.py"
      - "AIFE/staging/tests/integration/server/test_f5c_acquisition_durable_acceptance.py"
      - "AIFE/staging/tests/integration/server/test_f5c_acquisition_extensibility.py"
      - "AIFE/staging/tests/integration/server/test_f5_vertical_slice.py"
      - "AIFE/staging/tests/integration/server/test_f5_publication_integrity.py"
      - "AIFE/staging/tests/integration/server/test_f5_publication_recovery.py"
      - "AIFE/staging/tests/integration/server/test_f5_sqlite_control.py"
      - ".github/workflows/qualify-d8-runtime.yml"
      - "bridge-contract.json"
      - "tools/history_consumer.py"
      - "tools/history_access_v2.py"
      - "tools/history_issue_request.py"
      - "tools/validation/validate_research_semantic_receipt_compat.py"
      - "tools/validation/validate_r2_p1_auth_scope_authority.py"
      - "tests/deep_history/test_history_consumer.py"
      - "tests/deep_history/test_history_issue_request.py"
      - "tests/deep_history/test_semantic_receipt_contract.py"
      - "tests/deep_history/test_release_publisher.py"
      - "tests/fixtures/research-d9-5/market-data-ref.schema.json"
      - ".github/workflows/history-consumer-read.yml"
  push:
    paths:
      - "AGENTS.md"
      - "contracts/d8-runtime-candidate.json"
      - "docs/semantics/d8-vps-unified-acquisition-runtime-v1.md"
      - "docs/semantics/d8-production-capability-parity-expansion-and-cutover-v1.md"
      - "docs/handoffs/d8-vps-runtime-integration-handoff-v1.md"
      - "tools/d8/Dockerfile"
      - "src/d8_runtime.py"
      - "src/d8_service.py"
      - "src/acquisition_core.py"
      - "src/d8_observation_normalizer.py"
      - "src/aife_server_adapter.py"
      - "src/aife_f5c_acquisition_adapter.py"
      - "tests/d8/**"
      - "AIFE/staging/server/acquisition/**"
      - "AIFE/staging/server/integration/bindings.py"
      - "AIFE/staging/server/runtime/composition.py"
      - "AIFE/staging/server/runtime/deployment.py"
      - "AIFE/staging/server/runtime/c9_forward.py"
      - "AIFE/staging/deploy/server/installer/aife_deploy.py"
      - "AIFE/staging/tests/integration/server/test_privileged_deployment_executor_integration.py"
      - "AIFE/staging/tests/unit/server/test_privileged_deployment_executor.py"
      - "AIFE/staging/tests/unit/server/test_bootstrap_package_renderer.py"
      - "AIFE/staging/tests/integration/server/test_f5_permissions_and_deployment.py"
      - "AIFE/staging/tests/integration/server/test_f5c_acquisition_durable_acceptance.py"
      - "AIFE/staging/tests/integration/server/test_f5c_acquisition_extensibility.py"
      - "AIFE/staging/tests/integration/server/test_f5_vertical_slice.py"
      - "AIFE/staging/tests/integration/server/test_f5_publication_integrity.py"
      - "AIFE/staging/tests/integration/server/test_f5_publication_recovery.py"
      - "AIFE/staging/tests/integration/server/test_f5_sqlite_control.py"
      - ".github/workflows/qualify-d8-runtime.yml"
      - "bridge-contract.json"
      - "tools/history_consumer.py"
      - "tools/history_access_v2.py"
      - "tools/history_issue_request.py"
      - "tools/validation/validate_research_semantic_receipt_compat.py"
      - "tools/validation/validate_r2_p1_auth_scope_authority.py"
      - "tests/deep_history/test_history_consumer.py"
      - "tests/deep_history/test_history_issue_request.py"
      - "tests/deep_history/test_semantic_receipt_contract.py"
      - "tests/deep_history/test_release_publisher.py"
      - "tests/fixtures/research-d9-5/market-data-ref.schema.json"
      - ".github/workflows/history-consumer-read.yml"

permissions:
  contents: read

concurrency:
  group: eth-macro-d8-runtime-${{ github.ref }}
  cancel-in-progress: true

jobs:
  source-and-container:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      PYTHONPATH: .:src:tools:tools/deep_history:AIFE/staging
    steps:
      - name: Checkout exact current ref
        uses: actions/checkout@v6
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0

      - name: Prove C6 additive history objects available
        shell: bash
        run: |
          set -euo pipefail
          git cat-file -e 9564082c20030a9d2f4c9b4556a26101f22b46ff^{commit}
          git cat-file -e 971424a04187b2442d7b49ec07dadadd8e432e00^{commit}
          git cat-file -e 803f6bac33edd8315693d8ff4065965c9d03c464^{commit}
          echo "C6_FROZEN_BASE_OBJECT_AVAILABLE=PASS"
          echo "C6_INITIAL_SOURCE_OBJECT_AVAILABLE=PASS"
          echo "C6_FIRST_REPAIR_OBJECT_AVAILABLE=PASS"

      - name: Bind qualification provenance to exact checkout
        shell: bash
        env:
          PR_BASE_SHA_FROM_EVENT: ${{ github.event.pull_request.base.sha || '' }}
          PR_HEAD_SHA_FROM_EVENT: ${{ github.event.pull_request.head.sha || '' }}
        run: |
          set -euo pipefail
          QUALIFIED_CHECKOUT_SHA="$(git rev-parse HEAD)"
          QUALIFIED_CHECKOUT_TREE="$(git rev-parse 'HEAD^{tree}')"
          test -n "$QUALIFIED_CHECKOUT_SHA"
          test -n "$QUALIFIED_CHECKOUT_TREE"
          ACTUAL="$(git rev-parse HEAD)"
          test "$ACTUAL" = "$QUALIFIED_CHECKOUT_SHA"

          PR_HEAD_SHA="${PR_HEAD_SHA_FROM_EVENT:-$GITHUB_SHA}"
          RESEARCH_BRIDGE_HEAD_SHA="${PR_HEAD_SHA_FROM_EVENT:-$GITHUB_SHA}"
          test -n "$PR_HEAD_SHA"
          test -n "$RESEARCH_BRIDGE_HEAD_SHA"

          printf 'QUALIFIED_CHECKOUT_SHA=%s\n' "$QUALIFIED_CHECKOUT_SHA" >> "$GITHUB_ENV"
          printf 'QUALIFIED_CHECKOUT_TREE=%s\n' "$QUALIFIED_CHECKOUT_TREE" >> "$GITHUB_ENV"
          printf 'PR_HEAD_SHA=%s\n' "$PR_HEAD_SHA" >> "$GITHUB_ENV"
          printf 'RESEARCH_BRIDGE_HEAD_SHA=%s\n' "$RESEARCH_BRIDGE_HEAD_SHA" >> "$GITHUB_ENV"

          printf 'GITHUB_EVENT_NAME=%s\n' "$GITHUB_EVENT_NAME"
          printf 'GITHUB_REF=%s\n' "$GITHUB_REF"
          printf 'GITHUB_EVENT_SHA=%s\n' "$GITHUB_SHA"
          printf 'PR_HEAD_SHA=%s\n' "$PR_HEAD_SHA"
          printf 'QUALIFIED_CHECKOUT_SHA=%s\n' "$QUALIFIED_CHECKOUT_SHA"
          printf 'QUALIFIED_CHECKOUT_TREE=%s\n' "$QUALIFIED_CHECKOUT_TREE"
          printf 'ACTUAL_GIT_HEAD=%s\n' "$ACTUAL"

          if [[ "$GITHUB_EVENT_NAME" == "pull_request" ]]; then
            read -r -a parents <<< "$(git show -s --format=%P HEAD)"
            test "${#parents[@]}" -eq 2
            test "${parents[0]}" = "$PR_BASE_SHA_FROM_EVENT"
            test "${parents[1]}" = "$PR_HEAD_SHA_FROM_EVENT"
            echo "PR_BASE_SHA=$PR_BASE_SHA_FROM_EVENT"
            echo "PR_HEAD_SHA=$PR_HEAD_SHA_FROM_EVENT"
            echo "PARENT_1=${parents[0]}"
            echo "PARENT_2=${parents[1]}"
            echo "PR_EFFECTIVE_INTEGRATION_BINDING=PASS"
          else
            echo "PR_EFFECTIVE_INTEGRATION_BINDING=N/A"
          fi

          echo "ACTUAL_GIT_HEAD_MATCH=PASS"
          echo "ACTUAL_CHECKOUT_SHA_MATCH=PASS"
          echo "PHYSICAL_IDENTITY_PROOF=PASS"
          echo "QUALIFIED_CHECKOUT_SHA_ROLE=TESTED_BYTES"
          echo "PR_HEAD_SHA_ROLE=LOGICAL_PR_HEAD_OR_NON_PR_EVENT_SOURCE_FALLBACK"
          echo "GITHUB_SHA_ROLE=EVENT_IDENTITY_AND_TESTED_BYTES_AUTHORITY"

      - name: Classify exact F5C C6, C7, or C9 source repair qualification
        shell: bash
        run: |
          set -euo pipefail
          C6_FROZEN_BASE_HEAD="9564082c20030a9d2f4c9b4556a26101f22b46ff"
          C6_INITIAL_SOURCE_HEAD="971424a04187b2442d7b49ec07dadadd8e432e00"
          C6_FIRST_REPAIR_HEAD="803f6bac33edd8315693d8ff4065965c9d03c464"
          C6_ACCEPTED_HEAD="a5a0f7f0ac4fd0ce6fd19d9e14b079fa0d041dc9"
          C6_ACCEPTED_TREE="e6886d8c24e6452a2418e6e22d567d19960cd8a9"
          C7_ORIGINAL_BASE_PARENT="b645d34a992c0fc88d8cb09aed6fa6913c5190ad"
          C7_INITIAL_IMPLEMENTATION_HEAD="273653636de5190dcb80324148ddcdbe0a66b2a7"
          C7_INITIAL_IMPLEMENTATION_TREE="ab7909ccecb97ceed6f0173db028d42856872d05"
          C7_FIRST_REPAIR_HEAD="7817ac772ee3d4278c9e4dff347ce3936e523c84"
          C7_FIRST_REPAIR_TREE="93b5e56beef5a52c035db5fbf7f7d3f8f73ed354"
          C7_AUTHORITY_REPAIR_HEAD="9e8eb8d5b8e8a708e58a17da0d771dbe1b473a8c"
          C7_AUTHORITY_REPAIR_TREE="ff19972e33adb916fa5136f8d1ffa9720aca0785"
          C7_BRANCH_REF="refs/heads/agent/aife/server-data-foundation-wip"
          C7_DOCKERFILE_BLOB="3629fe6b0d824fd10a460d8a00c5c1d8503358cf"
          C7_CARRIER_BLOB="bcd2116885835661400c23c9856152dfd8a8b7e8"
          C9_AUTHORIZATION_BASE="487cb08d88ac20ef353106c7eb444a4a7495555b"
          C9_INITIAL_IMPLEMENTATION_HEAD="1c8209c21c363b977ba1c599495257c412485793"
          C9_SOURCE_REPAIR_QUALIFICATION=NO
          C9_SOURCE_REPAIR_SCOPE_CLASSIFICATION=NOT_APPLICABLE
          C9_SOURCE_REPAIR_ROUTE_KIND=NONE
          C9_WORKFLOW_REPAIR_CLASSIFICATION=NOT_APPLICABLE
          C9_WORKFLOW_REPAIR_OWN_DELTA_PATH_COUNT=-1
          C9_SOURCE_TEST_BLOB_MATCH_COUNT=0
          C9_UNAUTHORIZED_CHANGED_PATH_COUNT=-1
          C6_EXACT=NO
          C7_EXACT=NO
          C7_ROUTE_KIND=NONE
          AUTHORIZED_C7_REPAIR_SUCCESSOR=NO
          AUTHORIZED_C7_AUTHORITY_REPAIR_SUCCESSOR=NO
          AUTHORIZED_C7_STATE_ACCESS_REPAIR_SUCCESSOR=NO

          if [[ "$GITHUB_EVENT_NAME" == "push" \
                && "$(git rev-parse HEAD)" == "$C6_ACCEPTED_HEAD" \
                && "$(git rev-parse 'HEAD^{tree}')" == "$C6_ACCEPTED_TREE" ]]; then
            git merge-base --is-ancestor "$C6_FROZEN_BASE_HEAD" HEAD
            git merge-base --is-ancestor "$C6_INITIAL_SOURCE_HEAD" HEAD
            git merge-base --is-ancestor "$C6_FIRST_REPAIR_HEAD" HEAD
            test "$(git rev-parse HEAD^)" = "$C6_FIRST_REPAIR_HEAD"
            git diff --quiet "$C6_INITIAL_SOURCE_HEAD" HEAD -- AIFE/staging/server/runtime/deployment.py
            git diff --quiet "$C6_INITIAL_SOURCE_HEAD" HEAD -- AIFE/staging/tests/integration/server/test_f5_permissions_and_deployment.py
            expected="$(printf '%s\n' \
              '.github/workflows/qualify-d8-runtime.yml' \
              'AIFE/staging/server/runtime/deployment.py' \
              'AIFE/staging/tests/integration/server/test_f5_permissions_and_deployment.py' | sort)"
            actual="$(git diff --name-only "$C6_FROZEN_BASE_HEAD" HEAD | sort)"
            test "$actual" = "$expected"
            C6_EXACT=YES
            echo "C6_FROZEN_BASE_ANCESTRY=PASS"
            echo "C6_INITIAL_SOURCE_ANCESTRY=PASS"
            echo "C6_FIRST_REPAIR_ANCESTRY=PASS"
            echo "C6_SECOND_REPAIR_DIRECT_PARENT=PASS"
            echo "C6_CUMULATIVE_SOURCE_DIFF_BOUNDARY=PASS"
            echo "C6_CUMULATIVE_SOURCE_CHANGED_PATH_COUNT=3"
          elif [[ "$GITHUB_EVENT_NAME" == "push" \
                  && "$GITHUB_REF" == "$C7_BRANCH_REF" \
                  && "$(git rev-parse HEAD^)" == "$C9_AUTHORIZATION_BASE" ]]; then
            expected="$(printf '%s\n' \
              '.github/workflows/qualify-d8-runtime.yml' \
              'AIFE/staging/deploy/server/installer/aife_deploy.py' \
              'AIFE/staging/server/runtime/c9_forward.py' \
              'AIFE/staging/server/runtime/deployment.py' \
              'AIFE/staging/tests/integration/server/test_f5_permissions_and_deployment.py' \
              'AIFE/staging/tests/integration/server/test_privileged_deployment_executor_integration.py' \
              'tests/d8/test_f5c_aife_acquisition_bridge.py' | sort)"
            actual="$(git diff --name-only "$C9_AUTHORIZATION_BASE" HEAD | sort)"
            if [[ "$actual" != "$expected" ]]; then
              echo "C9_SOURCE_REPAIR_SCOPE_CLASSIFICATION=FAIL"
              exit 1
            fi
            added="$(git diff --diff-filter=A --name-only "$C9_AUTHORIZATION_BASE" HEAD | sort)"
            test "$added" = 'AIFE/staging/server/runtime/c9_forward.py'
            C9_UNAUTHORIZED_CHANGED_PATH_COUNT=0
            C9_SOURCE_REPAIR_QUALIFICATION=YES
            C9_SOURCE_REPAIR_SCOPE_CLASSIFICATION=PASS
            C9_SOURCE_REPAIR_ROUTE_KIND=INITIAL_IMPLEMENTATION
            echo "C9_SOURCE_REPAIR_EXPECTED_PARENT=$C9_AUTHORIZATION_BASE"
            echo "C9_SOURCE_REPAIR_SCOPE_CLASSIFICATION=PASS"
            echo "UNAUTHORIZED_CHANGED_PATH_COUNT=0"
            echo "C9_SOURCE_REPAIR_CHANGED_PATH_COUNT=7"
          elif [[ "$GITHUB_EVENT_NAME" == "push" \
                  && "$GITHUB_REF" == "$C7_BRANCH_REF" \
                  && "$(git rev-parse HEAD^)" == "$C9_INITIAL_IMPLEMENTATION_HEAD" ]]; then
            workflow_repair_expected="$(printf 'M\t%s\n' '.github/workflows/qualify-d8-runtime.yml')"
            workflow_repair_actual="$(git diff --name-status "$C9_INITIAL_IMPLEMENTATION_HEAD" HEAD | sort)"
            test "$workflow_repair_actual" = "$workflow_repair_expected"
            C9_WORKFLOW_REPAIR_OWN_DELTA_PATH_COUNT="$(git diff --name-only "$C9_INITIAL_IMPLEMENTATION_HEAD" HEAD | wc -l)"
            test "$C9_WORKFLOW_REPAIR_OWN_DELTA_PATH_COUNT" = "1"

            cumulative_expected="$(printf '%s\n' \
              '.github/workflows/qualify-d8-runtime.yml' \
              'AIFE/staging/deploy/server/installer/aife_deploy.py' \
              'AIFE/staging/server/runtime/c9_forward.py' \
              'AIFE/staging/server/runtime/deployment.py' \
              'AIFE/staging/tests/integration/server/test_f5_permissions_and_deployment.py' \
              'AIFE/staging/tests/integration/server/test_privileged_deployment_executor_integration.py' \
              'tests/d8/test_f5c_aife_acquisition_bridge.py' | sort)"
            cumulative_actual="$(git diff --name-only "$C9_AUTHORIZATION_BASE" HEAD | sort)"
            test "$cumulative_actual" = "$cumulative_expected"
            added="$(git diff --diff-filter=A --name-only "$C9_AUTHORIZATION_BASE" HEAD | sort)"
            test "$added" = 'AIFE/staging/server/runtime/c9_forward.py'

            C9_SOURCE_TEST_BLOB_MATCH_COUNT=0
            for source_test_path in \
              AIFE/staging/deploy/server/installer/aife_deploy.py \
              AIFE/staging/server/runtime/c9_forward.py \
              AIFE/staging/server/runtime/deployment.py \
              AIFE/staging/tests/integration/server/test_f5_permissions_and_deployment.py \
              AIFE/staging/tests/integration/server/test_privileged_deployment_executor_integration.py \
              tests/d8/test_f5c_aife_acquisition_bridge.py; do
              test "$(git rev-parse "$C9_INITIAL_IMPLEMENTATION_HEAD:$source_test_path")" = "$(git rev-parse "HEAD:$source_test_path")"
              C9_SOURCE_TEST_BLOB_MATCH_COUNT=$((C9_SOURCE_TEST_BLOB_MATCH_COUNT+1))
            done
            test "$C9_SOURCE_TEST_BLOB_MATCH_COUNT" = "6"

            C9_UNAUTHORIZED_CHANGED_PATH_COUNT=0
            C9_SOURCE_REPAIR_QUALIFICATION=YES
            C9_SOURCE_REPAIR_SCOPE_CLASSIFICATION=PASS
            C9_SOURCE_REPAIR_ROUTE_KIND=AUTHORIZED_WORKFLOW_REPAIR_SUCCESSOR
            C9_WORKFLOW_REPAIR_CLASSIFICATION=PASS
            echo "C9_WORKFLOW_REPAIR_CLASSIFICATION=PASS"
            echo "C9_WORKFLOW_REPAIR_OWN_DELTA_PATH_COUNT=$C9_WORKFLOW_REPAIR_OWN_DELTA_PATH_COUNT"
            echo "C9_WORKFLOW_REPAIR_OWN_DELTA=PASS"
            echo "C9_CUMULATIVE_AUTHORIZED_PATH_COUNT=7"
            echo "C9_CUMULATIVE_AUTHORIZED_SCOPE=PASS"
            echo "SOURCE_TEST_BLOB_MATCH_COUNT=${C9_SOURCE_TEST_BLOB_MATCH_COUNT}_OF_6"
            echo "SOURCE_TEST_BLOB_DRIFT_COUNT=0"
            echo "UNAUTHORIZED_CHANGED_PATH_COUNT=0"
          elif [[ "$GITHUB_EVENT_NAME" == "push" \
                  && "$GITHUB_REF" == "$C7_BRANCH_REF" ]]; then
            C7_DIRECT_PARENT="$(git rev-parse HEAD^)"
            if [[ "$C7_DIRECT_PARENT" == "$C7_ORIGINAL_BASE_PARENT" ]]; then
              C7_ROUTE_KIND=INITIAL_IMPLEMENTATION
              echo "C7_INITIAL_DIRECT_PARENT=PASS"
            elif [[ "$C7_DIRECT_PARENT" == "$C7_INITIAL_IMPLEMENTATION_HEAD" ]]; then
              test "$(git rev-parse "$C7_INITIAL_IMPLEMENTATION_HEAD^{tree}")" = "$C7_INITIAL_IMPLEMENTATION_TREE"
              repair_expected="$(printf 'M\t%s\n' '.github/workflows/qualify-d8-runtime.yml')"
              repair_actual="$(git diff --name-status "$C7_INITIAL_IMPLEMENTATION_HEAD" HEAD | sort)"
              test "$repair_actual" = "$repair_expected"
              AUTHORIZED_C7_REPAIR_SUCCESSOR=YES
              C7_ROUTE_KIND=AUTHORIZED_REPAIR_SUCCESSOR
              echo "C7_REPAIR_DIRECT_PARENT=PASS"
              echo "C7_REPAIR_OWN_DELTA_1PATH=PASS"
              echo "AUTHORIZED_C7_REPAIR_SUCCESSOR=YES"
            elif [[ "$C7_DIRECT_PARENT" == "$C7_FIRST_REPAIR_HEAD" ]]; then
              test "$(git rev-parse "$C7_FIRST_REPAIR_HEAD^{tree}")" = "$C7_FIRST_REPAIR_TREE"
              authority_repair_expected="$(printf 'M\t%s\n' '.github/workflows/qualify-d8-runtime.yml')"
              authority_repair_actual="$(git diff --name-status "$C7_FIRST_REPAIR_HEAD" HEAD | sort)"
              test "$authority_repair_actual" = "$authority_repair_expected"
              AUTHORIZED_C7_AUTHORITY_REPAIR_SUCCESSOR=YES
              C7_ROUTE_KIND=AUTHORIZED_C7_AUTHORITY_REPAIR_SUCCESSOR
              echo "C7_AUTHORITY_REPAIR_DIRECT_PARENT=PASS"
              echo "C7_AUTHORITY_REPAIR_OWN_DELTA_1PATH=PASS"
              echo "AUTHORIZED_C7_AUTHORITY_REPAIR_SUCCESSOR=YES"
            elif [[ "$C7_DIRECT_PARENT" == "$C7_AUTHORITY_REPAIR_HEAD" ]]; then
              test "$(git rev-parse "$C7_AUTHORITY_REPAIR_HEAD^{tree}")" = "$C7_AUTHORITY_REPAIR_TREE"
              state_access_repair_expected="$(printf 'M\t%s\n' '.github/workflows/qualify-d8-runtime.yml')"
              state_access_repair_actual="$(git diff --name-status "$C7_AUTHORITY_REPAIR_HEAD" HEAD | sort)"
              test "$state_access_repair_actual" = "$state_access_repair_expected"
              AUTHORIZED_C7_STATE_ACCESS_REPAIR_SUCCESSOR=YES
              C7_ROUTE_KIND=AUTHORIZED_C7_STATE_ACCESS_REPAIR_SUCCESSOR
              echo "C7_STATE_ACCESS_REPAIR_DIRECT_PARENT=PASS"
              echo "C7_STATE_ACCESS_REPAIR_OWN_DELTA_1PATH=PASS"
              echo "AUTHORIZED_C7_STATE_ACCESS_REPAIR_SUCCESSOR=YES"
            fi

            if [[ "$C7_ROUTE_KIND" != NONE ]]; then
              expected="$(
                {
                  printf 'M\t%s\n' '.github/workflows/qualify-d8-runtime.yml'
                  printf 'A\t%s\n' 'AIFE/staging/server/Dockerfile.c7'
                  printf 'A\t%s\n' 'AIFE/staging/server/runtime/c7_carrier.py'
                } | sort
              )"
              actual="$(git diff --name-status "$C7_ORIGINAL_BASE_PARENT" HEAD | sort)"
              test "$actual" = "$expected"
              test "$(git rev-parse HEAD:AIFE/staging/server/Dockerfile.c7)" = "$C7_DOCKERFILE_BLOB"
              test "$(git rev-parse HEAD:AIFE/staging/server/runtime/c7_carrier.py)" = "$C7_CARRIER_BLOB"
              git diff --quiet "$C6_ACCEPTED_HEAD" HEAD -- AIFE/staging/core
              git diff --quiet "$C6_ACCEPTED_HEAD" HEAD -- AIFE/staging/tests
              server_delta="$(git diff --name-status "$C6_ACCEPTED_HEAD" HEAD -- AIFE/staging/server | sort)"
              expected_server="$(
                {
                  printf 'A\t%s\n' 'AIFE/staging/server/Dockerfile.c7'
                  printf 'A\t%s\n' 'AIFE/staging/server/runtime/c7_carrier.py'
                } | sort
              )"
              test "$server_delta" = "$expected_server"
              C7_EXACT=YES
              echo "C7_CUMULATIVE_EXACT_3PATH_DELTA=PASS"
              echo "C7_FROZEN_IMPLEMENTATION_BLOBS_UNCHANGED=PASS"
              echo "C7_CORE_UNCHANGED_FROM_C6=PASS"
              echo "C7_TESTS_UNCHANGED_FROM_C6=PASS"
              echo "C7_SERVER_DELTA_EXACT=PASS"
            fi
          fi

          classification_count=0
          if [[ "$C6_EXACT" == YES ]]; then classification_count=$((classification_count+1)); fi
          if [[ "$C7_EXACT" == YES ]]; then classification_count=$((classification_count+1)); fi
          if [[ "$C9_SOURCE_REPAIR_QUALIFICATION" == YES ]]; then classification_count=$((classification_count+1)); fi
          if [[ "$classification_count" -gt 1 ]]; then
            echo "F5C_EXACT_CLASSIFICATION_MUTUAL_EXCLUSION=FAIL"
            exit 1
          fi
          echo "C6_SOURCE_QUALIFICATION=$C6_EXACT" >> "$GITHUB_ENV"
          echo "C6_EXACT=$C6_EXACT" >> "$GITHUB_ENV"
          echo "C7_EXACT=$C7_EXACT" >> "$GITHUB_ENV"
          echo "C7_ROUTE_KIND=$C7_ROUTE_KIND" >> "$GITHUB_ENV"
          echo "C9_SOURCE_REPAIR_QUALIFICATION=$C9_SOURCE_REPAIR_QUALIFICATION" >> "$GITHUB_ENV"
          echo "C9_SOURCE_REPAIR_SCOPE_CLASSIFICATION=$C9_SOURCE_REPAIR_SCOPE_CLASSIFICATION" >> "$GITHUB_ENV"
          echo "C9_SOURCE_REPAIR_ROUTE_KIND=$C9_SOURCE_REPAIR_ROUTE_KIND" >> "$GITHUB_ENV"
          echo "C9_WORKFLOW_REPAIR_CLASSIFICATION=$C9_WORKFLOW_REPAIR_CLASSIFICATION" >> "$GITHUB_ENV"
          echo "C9_WORKFLOW_REPAIR_OWN_DELTA_PATH_COUNT=$C9_WORKFLOW_REPAIR_OWN_DELTA_PATH_COUNT" >> "$GITHUB_ENV"
          echo "C9_SOURCE_TEST_BLOB_MATCH_COUNT=$C9_SOURCE_TEST_BLOB_MATCH_COUNT" >> "$GITHUB_ENV"
          echo "C9_UNAUTHORIZED_CHANGED_PATH_COUNT=$C9_UNAUTHORIZED_CHANGED_PATH_COUNT" >> "$GITHUB_ENV"
          echo "AUTHORIZED_C7_REPAIR_SUCCESSOR=$AUTHORIZED_C7_REPAIR_SUCCESSOR" >> "$GITHUB_ENV"
          echo "AUTHORIZED_C7_AUTHORITY_REPAIR_SUCCESSOR=$AUTHORIZED_C7_AUTHORITY_REPAIR_SUCCESSOR" >> "$GITHUB_ENV"
          echo "AUTHORIZED_C7_STATE_ACCESS_REPAIR_SUCCESSOR=$AUTHORIZED_C7_STATE_ACCESS_REPAIR_SUCCESSOR" >> "$GITHUB_ENV"
          echo "C6_EXACT=$C6_EXACT"
          echo "C7_EXACT=$C7_EXACT"
          echo "C7_ROUTE_KIND=$C7_ROUTE_KIND"
          echo "C9_SOURCE_REPAIR_QUALIFICATION=$C9_SOURCE_REPAIR_QUALIFICATION"
          echo "C9_SOURCE_REPAIR_SCOPE_CLASSIFICATION=$C9_SOURCE_REPAIR_SCOPE_CLASSIFICATION"
          echo "C9_SOURCE_REPAIR_ROUTE_KIND=$C9_SOURCE_REPAIR_ROUTE_KIND"
          echo "C9_WORKFLOW_REPAIR_CLASSIFICATION=$C9_WORKFLOW_REPAIR_CLASSIFICATION"
          if [[ "$classification_count" -eq 0 ]]; then
            echo "F5C_EXACT_CLASSIFICATION=OTHER"
          fi
          echo "F5C_EXACT_CLASSIFICATION_MUTUAL_EXCLUSION=PASS"

      - name: Prove planning-only or authorized C6 diff boundary
        if: github.event_name == 'pull_request'
        shell: bash
        run: |
          set -euo pipefail
          changed="$(git diff --name-only HEAD^1 HEAD^2)"
          printf '%s\n' "$changed"
          if [[ "$C6_SOURCE_QUALIFICATION" == "YES" ]]; then
            allowed='^(\.github/workflows/qualify-d8-runtime\.yml|AIFE/staging/server/runtime/deployment\.py|AIFE/staging/tests/integration/server/test_f5_permissions_and_deployment\.py)$'
            unexpected="$(printf '%s\n' "$changed" | grep -Ev "$allowed" || true)"
            test -z "$unexpected"
            echo "C6_AUTHORIZED_RUNTIME_SOURCE_DIFF=PASS"
            echo "RUNTIME_SOURCE_MUTATION=C6_AUTHORIZED"
            echo "RESEARCH_MUTATION=NO"
          else
            allowed='^(\.github/workflows/qualify-d8-runtime\.yml|docs/semantics/d8-production-capability-parity-expansion-and-cutover-v1\.md|tools/validation/validate_r2_p1_auth_scope_authority\.py)$'
            unexpected="$(printf '%s\n' "$changed" | grep -Ev "$allowed" || true)"
            test -z "$unexpected"
            echo "NO_RUNTIME_SOURCE_DIFF=PASS"
            echo "RUNTIME_SOURCE_MUTATION=NO"
            echo "RESEARCH_MUTATION=NO"
          fi

      - name: Set up Python 3.11
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install F5 integration test dependency
        run: python -m pip install pytest

      - name: Run F5C F5 integration qualification
        run: |
          python -m pytest -q \
            AIFE/staging/tests/integration/server/test_f5_permissions_and_deployment.py \
            AIFE/staging/tests/integration/server/test_f5c_acquisition_durable_acceptance.py \
            AIFE/staging/tests/integration/server/test_f5c_acquisition_extensibility.py \
            AIFE/staging/tests/integration/server/test_f5_vertical_slice.py \
            AIFE/staging/tests/integration/server/test_f5_publication_integrity.py \
            AIFE/staging/tests/integration/server/test_f5_publication_recovery.py \
            AIFE/staging/tests/integration/server/test_f5_sqlite_control.py
          echo "F5_PERMISSIONS_AND_DEPLOYMENT_TESTS=PASS"
          echo "NEGATIVE_DEPLOYMENT_IDENTITY_PROOFS=PASS"

      - name: Qualify F5C C9 source repair exact SHA
        if: env.C9_SOURCE_REPAIR_QUALIFICATION == 'YES'
        shell: bash
        run: |
          set -euo pipefail
          case "$C9_SOURCE_REPAIR_ROUTE_KIND" in
            INITIAL_IMPLEMENTATION)
              test "$(git rev-parse HEAD^)" = "487cb08d88ac20ef353106c7eb444a4a7495555b"
              ;;
            AUTHORIZED_WORKFLOW_REPAIR_SUCCESSOR)
              test "$(git rev-parse HEAD^)" = "1c8209c21c363b977ba1c599495257c412485793"
              test "$C9_WORKFLOW_REPAIR_CLASSIFICATION" = "PASS"
              test "$C9_WORKFLOW_REPAIR_OWN_DELTA_PATH_COUNT" = "1"
              test "$C9_SOURCE_TEST_BLOB_MATCH_COUNT" = "6"
              ;;
            *)
              echo "C9_SOURCE_REPAIR_ROUTE_KIND=FAIL"
              exit 1
              ;;
          esac
          test "$C9_SOURCE_REPAIR_SCOPE_CLASSIFICATION" = "PASS"
          test "$C9_UNAUTHORIZED_CHANGED_PATH_COUNT" = "0"

          python -m pytest -q \
            AIFE/staging/tests/integration/server/test_f5_permissions_and_deployment.py \
            AIFE/staging/tests/integration/server/test_privileged_deployment_executor_integration.py \
            tests/d8/test_f5c_aife_acquisition_bridge.py \
            AIFE/staging/tests/integration/server/test_f5c_acquisition_durable_acceptance.py \
            AIFE/staging/tests/integration/server/test_f5c_acquisition_extensibility.py \
            AIFE/staging/tests/integration/server/test_f5_vertical_slice.py \
            AIFE/staging/tests/integration/server/test_f5_publication_integrity.py \
            AIFE/staging/tests/integration/server/test_f5_publication_recovery.py \
            AIFE/staging/tests/integration/server/test_f5_sqlite_control.py
          echo "C9_ROOT_CONTEXT_TESTS=PASS"
          echo "C9_FORWARD_FAKE_PROVIDER_ROUTE=PASS"
          echo "DURABLE_ACCEPTANCE=PASS"
          echo "SAME_WORK_REUSE=PASS"
          echo "PUBLICATION_GENERATION_ACCESS=PASS"
          echo "END_TO_END_BYTE_IDENTITY=PASS"
          echo "REAL_PROVIDER_NETWORK_CALL=NO"

          (
            cd AIFE/staging
            PYTHONPATH=.:../../src:../../tools:../../tools/deep_history \
              python -m pytest -q tests/unit/server/test_privileged_deployment_executor.py
            echo "EXECUTOR_UNIT_TESTS=PASS"
            PYTHONPATH=.:../../src:../../tools:../../tools/deep_history \
              python -m pytest -q tests/unit/server/test_bootstrap_package_renderer.py
            echo "BOOTSTRAP_RENDERER_UNIT_TESTS=PASS"
          )

          C9_PROJECTION_EVIDENCE="$RUNNER_TEMP/c9-source-repair-projection.json"
          python -m server.runtime.deployment qualify \
            --repo-root . \
            --expected-head "$QUALIFIED_CHECKOUT_SHA" \
            --expected-tree "$QUALIFIED_CHECKOUT_TREE" \
            --evidence-output "$C9_PROJECTION_EVIDENCE"
          python - "$C9_PROJECTION_EVIDENCE" <<'PY'
          import hashlib,json,sys
          from pathlib import Path
          from deploy.server.installer import aife_deploy
          from server.runtime import deployment
          evidence=json.loads(Path(sys.argv[1]).read_text())
          entries=evidence['projected_entries']
          projected=[row['PROJECTED_PATH'] for row in entries]
          assert len(projected)==len(set(projected)), 'projected collision'
          src=[row for row in entries if row['SOURCE_PATH'].startswith('src/')]
          assert src, 'canonical src projection missing'
          assert all(row['BYTE_IDENTITY_MATCH']=='PASS' for row in src)
          assert evidence['MATERIALIZED_SOURCE_BYTE_IDENTITY']=='PASS'
          assert evidence['IMMUTABLE_RELEASE_MATERIALIZATION']=='PASS'
          assert evidence['RELEASE_GIT_BINDING']=='PASS'
          digest=hashlib.sha256(Path(deployment.__file__).read_bytes()).hexdigest()
          assert digest==aife_deploy.TRUSTED_CORE_SHA256
          print('AUTHORIZED_PATH_SET=PASS')
          print('CANONICAL_SRC_PROJECTION=PASS')
          print('PROJECTED_COLLISION_NEGATIVE_CONTROL=PASS')
          print('EXACT_GIT_BYTE_IDENTITY=PASS')
          print('RELEASE_MANIFEST=PASS')
          print('IMMUTABLE_RELEASE_MATERIALIZATION=PASS')
          print('RELEASE_GIT_BINDING=PASS')
          print('TRUSTED_CORE_EXECUTOR_BINDING=PASS')
          print('TERMINAL_C9_SOURCE_REPAIR_QUALIFICATION_ASSERTIONS=PASS')
          PY

      - name: Qualify F5C C6 exact Git deployment primitive
        if: env.C6_SOURCE_QUALIFICATION == 'YES'
        shell: bash
        run: |
          set -euo pipefail
          C6_EVIDENCE_PATH="$RUNNER_TEMP/c6-deployment-evidence.json"
          export C6_EVIDENCE_PATH
          printf 'C6_EVIDENCE_PATH=%s\n' "$C6_EVIDENCE_PATH" >> "$GITHUB_ENV"
          echo "C6_EVIDENCE_PATH=$C6_EVIDENCE_PATH"
          python -m server.runtime.deployment qualify \
            --repo-root . \
            --expected-head "$QUALIFIED_CHECKOUT_SHA" \
            --expected-tree "$QUALIFIED_CHECKOUT_TREE" \
            --evidence-output "$C6_EVIDENCE_PATH"
          python - <<'PY'
          import json
          import os
          from pathlib import Path
          evidence=json.loads(
              Path(os.environ["C6_EVIDENCE_PATH"]).read_text(encoding="utf-8")
          )
          required={
              'ACTUAL_GIT_HEAD_MATCH':'PASS',
              'ACTUAL_GIT_TREE_MATCH':'PASS',
              'ACTUAL_CHECKOUT_SHA_MATCH':'PASS',
              'PHYSICAL_IDENTITY_PROOF':'PASS',
              'C6_EXECUTABLE_DEPLOYMENT_PRIMITIVE':'PASS',
              'MATERIALIZED_SOURCE_BYTE_IDENTITY':'PASS',
              'IMMUTABLE_RELEASE_MATERIALIZATION':'PASS',
              'RELEASE_IDENTITY':'PASS',
              'RELEASE_GIT_BINDING':'PASS',
              'DEPLOYMENT_MAP_BINDING':'PASS',
              'DEPLOYMENT_RECEIPT_BINDING':'PASS',
              'DEPLOYMENT_RECEIPT_DURABLE_READBACK':'PASS',
              'ATOMIC_RELEASE_ACTIVATION_PROOF':'PASS',
              'F5_READINESS':'PASS',
              'QUALIFICATION_IS_ACTIVATION':'NO',
              'REAL_SERVER_ACTIVATION':'NO',
              'SHADOW_ACTIVATION':'NO',
              'PRODUCTION_ACTIVATION':'NO',
          }
          assert evidence['QUALIFIED_CHECKOUT_SHA'] == __import__('os').environ['QUALIFIED_CHECKOUT_SHA']
          assert evidence['QUALIFIED_CHECKOUT_TREE'] == __import__('os').environ['QUALIFIED_CHECKOUT_TREE']
          assert evidence['MATERIALIZED_PATH_COUNT'] > 0
          for key,value in required.items():
              assert evidence[key] == value, (key,evidence.get(key))
          print('C6_EXACT_SHA_DEPLOYMENT_EVIDENCE=PASS')
          print('F5_READINESS=PASS')
          print('NEGATIVE_DEPLOYMENT_IDENTITY_PROOFS=PASS')
          print('DOCKER_QUALIFICATION=NO')
          print('C7_STARTED=NO')
          PY

      - name: Prove active D6 D9 and legacy GitHub boundaries
        shell: bash
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          bridge=json.loads(Path('bridge-contract.json').read_text())
          d8=json.loads(Path('contracts/d8-runtime-candidate.json').read_text())
          assert bridge['semantic_resolution']['resolver']['resolution_plan_schema']=='market-data-resolution-plan/1.0.0'
          assert bridge['disabled_providers']['binance-usdm']['status']=='DISABLED_BY_POLICY'
          assert bridge['disabled_providers']['binance-usdm']['network_calls']==0
          assert bridge['disabled_providers']['binance-usdm']['vps_runtime_status']=='NOT_ACTIVE'
          assert d8['status']=='SOURCE_CANDIDATE_NOT_DEPLOYED'
          assert d8['authority']['d9_active'] is False
          assert d8['authority']['active_default_route']=='D6_RESOLUTION_PLAN_V1'
          assert d8['authority']['provider_authority_transition_allowed'] is False
          workflow=Path('.github/workflows/update-market.yml').read_text()
          assert 'cron: "35 * * * *"' in workflow
          assert '*/5' not in workflow
          assert 'python src/collector.py' in workflow
          print('D6_ACTIVE_ROUTE_COMPATIBILITY=PASS')
          print('D9_SOURCE_COMPATIBILITY=PASS')
          print('EXISTING_GITHUB_PRODUCTION_SCHEDULE_DISABLED=false')
          print('GITHUB_5M_SCHEDULER_CREATED=false')
          print('NO_BINANCE_USDM_GITHUB_NETWORK_CALL=PASS')
          PY

      - name: Prove shared provider family
        run: |
          python - <<'PY'
          import acquisition_core, collector, intelligence
          assert acquisition_core.binance is collector.binance
          assert acquisition_core.kraken is collector.kraken
          assert acquisition_core.collect_binance is intelligence.collect_binance
          assert acquisition_core.collect_kraken is intelligence.collect_kraken
          assert acquisition_core.collect_deribit_perpetual is intelligence.collect_deribit_perpetual
          assert acquisition_core.collect_options is intelligence.collect_options
          assert acquisition_core.collect_liquidity is intelligence.collect_liquidity
          print('SHARED_PROVIDER_LOGIC=PASS')
          print('NO_SECOND_COLLECTOR_FAMILY=PASS')
          PY

      - name: Run targeted stale-window checkpoint and post-D9.5 regressions
        run: |
          python -m unittest tests.d8.test_stale_window_recovery -v
          python -m unittest tests.d8.test_checkpoint_recovery -v
          python -m unittest tests.deep_history.test_history_consumer tests.deep_history.test_history_issue_request tests.deep_history.test_semantic_receipt_contract tests.deep_history.test_d9_public_resolution_v2 -v

      - name: Compile D8 source
        run: python -m compileall -q src tools tests

      - name: Run D8 targeted provider failure concurrency restart tests
        run: python -m unittest discover -s tests/d8 -p 'test_*.py' -v

      - name: Prove C6 repository root ephemeral evidence absent
        if: env.C6_SOURCE_QUALIFICATION == 'YES'
        shell: bash
        run: |
          set -euo pipefail
          test -n "$C6_EVIDENCE_PATH"
          test -f "$C6_EVIDENCE_PATH"
          test ! -e c6-deployment-evidence.json
          echo "C6_REPOSITORY_ROOT_EPHEMERAL_EVIDENCE_ABSENT=PASS"

      - name: Run existing repository gates
        run: |
          python tools/validation/validate_repository.py
          python tools/validation/validate_d9_contracts.py
          python tools/validation/validate.py
          python tools/validation/validate_v4.py
          python tools/validation/validate_history.py
          python tools/validation/consumer_proof.py
          python tools/validation/validate_d9_resolution_v2.py
          python tools/validation/validate_d8_r1_parity.py
          python tools/validation/validate_r2_p1_auth_scope_authority.py
          python tools/capability_index.py validate
          python -m unittest discover -s tests/deep_history -p 'test_*.py' -v

      - name: Qualify D9.5 semantic receipt producer contract
        run: |
          python -m unittest tests.deep_history.test_history_consumer tests.deep_history.test_history_issue_request tests.deep_history.test_semantic_receipt_contract tests.deep_history.test_d9_public_resolution_v2 -v
          python tools/history_consumer.py read \
            --series-id spot.binance-spot.ETHUSDT.ohlcv.5m \
            --from 2026-08-17T14:00:00Z \
            --to 2026-08-17T15:00:00Z \
            --mode strict \
            --current-policy FINALIZED_ONLY \
            --format csv \
            --output candles.csv \
            --plan-output resolution-plan.json \
            --diagnostics-output diagnostics.json \
            --receipt-output receipt.json \
            --semantic-receipt-output semantic-receipt.json
          python - <<'PY2'
          import hashlib,json
          from pathlib import Path
          plan=json.loads(Path('resolution-plan.json').read_text())
          diag=json.loads(Path('diagnostics.json').read_text())
          legacy=json.loads(Path('receipt.json').read_text())
          receipt=json.loads(Path('semantic-receipt.json').read_text())
          assert diag['status']=='PASS'
          assert diag['rows']==diag['expected_rows']==12
          assert diag['gap_count']==0 and diag['duplicates']==0
          assert receipt==legacy['semantic_receipt']
          assert receipt['receipt_schema_version']=='history-access-receipt/2.0.0'
          assert receipt['series_id']=='spot.binance-spot.ETHUSDT.ohlcv.5m'
          assert receipt['request']=={'from_utc':'2026-08-17T14:00:00Z','to_utc':'2026-08-17T15:00:00Z','cutoff_utc':None,'mode':'strict','current_policy':'FINALIZED_ONLY'}
          assert receipt['resolution_plan_sha256']==plan['plan_sha256']==legacy['plan_sha256']
          assert receipt['observation_count']==12
          assert receipt['finality']=='FINALIZED' and receipt['revision_context'] is None
          assert hashlib.sha256(Path('candles.csv').read_bytes()).hexdigest()==legacy['transport_output_sha256']==legacy['output_sha256']
          print('HISTORY_CONSUMER_E2E=PASS')
          print('ROWS=12')
          print('GAPS=0')
          print('DUPLICATES=0')
          print('SEMANTIC_RECEIPT_SCHEMA='+receipt['receipt_schema_version'])
          print('SERIES_ID_MATCH=PASS')
          print('REQUEST_IDENTITY_MATCH=PASS')
          print('PLAN_SHA_MATCH=PASS')
          print('OUTPUT_SHA_MATCH=PASS')
          print('OBSERVATION_COUNT_MATCH=PASS')
          print('FINALITY=FINALIZED')
          print('SEMANTIC_OUTPUT_SHA256='+receipt['output_sha256'])
          print('TRANSPORT_OUTPUT_SHA256='+legacy['transport_output_sha256'])
          PY2

      - name: Prove exact pinned Research semantic receipt compatibility
        shell: bash
        run: |
          echo "RESEARCH_BRIDGE_HEAD_IDENTITY_ROLE=LOGICAL_PR_OR_EVENT_SOURCE_HEAD"
          echo "RESEARCH_BRIDGE_HEAD_SHA=$RESEARCH_BRIDGE_HEAD_SHA"
          python tools/validation/validate_research_semantic_receipt_compat.py \
            --receipt semantic-receipt.json \
            --bridge-head "$RESEARCH_BRIDGE_HEAD_SHA"

      - name: Prepare F5C C7 qualification roots and authority
        if: env.C7_EXACT == 'YES'
        shell: bash
        run: |
          set -euo pipefail
          C7_TEMP_ROOT="$RUNNER_TEMP/aife-f5c-c7-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          C7_STATE_ROOT="$C7_TEMP_ROOT/state"
          C7_INPUT_ROOT="$C7_TEMP_ROOT/inputs"
          C7_EVIDENCE_ROOT="$C7_TEMP_ROOT/evidence"
          C7_IMAGE_REF="aife-f5c-c7:${QUALIFIED_CHECKOUT_SHA}"
          C7_CONTAINER_A="aife-c7-a-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          C7_CONTAINER_B="aife-c7-b-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          export C7_TEMP_ROOT C7_STATE_ROOT C7_INPUT_ROOT C7_EVIDENCE_ROOT
          mkdir -p "$C7_STATE_ROOT/data" "$C7_STATE_ROOT/control" "$C7_INPUT_ROOT" "$C7_EVIDENCE_ROOT"
          chmod 0777 "$C7_STATE_ROOT/data" "$C7_STATE_ROOT/control"
          for item in \
            "C7_TEMP_ROOT=$C7_TEMP_ROOT" \
            "C7_STATE_ROOT=$C7_STATE_ROOT" \
            "C7_INPUT_ROOT=$C7_INPUT_ROOT" \
            "C7_EVIDENCE_ROOT=$C7_EVIDENCE_ROOT" \
            "C7_IMAGE_REF=$C7_IMAGE_REF" \
            "C7_CONTAINER_A=$C7_CONTAINER_A" \
            "C7_CONTAINER_B=$C7_CONTAINER_B"; do
            echo "$item" >> "$GITHUB_ENV"
          done
          python - <<'PY'
          import json, os, subprocess
          from pathlib import Path
          root=Path(os.environ['C7_INPUT_ROOT'])
          common={
              'schema_version':'aife-f5c-c7-input/1.0.0',
              'artifact_type':'opaque-immutable',
              'payload_reference':'qualification-input',
              'provenance_reference':'qualification:c7',
              'acceptance_evidence_reference':'qualification:c7:runtime',
              'validated_at':'2026-09-09T00:00:00Z',
              'produced_at':'2026-09-09T00:00:00Z',
              'observed_at':'2026-09-09T00:00:00Z',
          }
          fixtures=[
              ('source-a.json','artifact-source-a-r1','source-a-r1','c291cmNlLWE=','0f9f5ce47831e099e77e295ed8bb627f089efa8672ee6fbdc49eac6f0d7f5275'),
              ('source-b.json','artifact-source-b-r9','source-b-r9','c291cmNlLWI=','3fed13457ee26a4f5b27c42544aa57045981a075a6103aab87e0b81c032e9d01'),
          ]
          for name,identity,revision,payload,digest in fixtures:
              value={**common,'artifact_identity':identity,'source_revision':revision,'payload_base64':payload,'content_identity_sha256':digest}
              (root/name).write_text(json.dumps(value,sort_keys=True,separators=(',',':'))+'\n',encoding='utf-8')
          evidence=Path(os.environ['C7_EVIDENCE_ROOT'])
          c7_original_base_parent='b645d34a992c0fc88d8cb09aed6fa6913c5190ad'
          c7_route_kind=os.environ['C7_ROUTE_KIND']
          immediate_parent=subprocess.check_output(['git','rev-parse','HEAD^'],text=True).strip()
          expected_immediate_parent={
              'INITIAL_IMPLEMENTATION':c7_original_base_parent,
              'AUTHORIZED_REPAIR_SUCCESSOR':'273653636de5190dcb80324148ddcdbe0a66b2a7',
              'AUTHORIZED_C7_AUTHORITY_REPAIR_SUCCESSOR':'7817ac772ee3d4278c9e4dff347ce3936e523c84',
              'AUTHORIZED_C7_STATE_ACCESS_REPAIR_SUCCESSOR':'9e8eb8d5b8e8a708e58a17da0d771dbe1b473a8c',
          }
          assert c7_route_kind in expected_immediate_parent
          assert immediate_parent==expected_immediate_parent[c7_route_kind]
          authority={
              'C6_EXACT':os.environ.get('C6_EXACT'),
              'C7_EXACT':os.environ.get('C7_EXACT'),
              'event':os.environ['GITHUB_EVENT_NAME'],
              'ref':os.environ['GITHUB_REF'],
              'qualified_checkout_sha':os.environ['QUALIFIED_CHECKOUT_SHA'],
              'qualified_checkout_tree':os.environ['QUALIFIED_CHECKOUT_TREE'],
              'c7_route_kind':c7_route_kind,
              'c7_original_base_parent':c7_original_base_parent,
              'immediate_parent':immediate_parent,
          }
          assert authority['C6_EXACT']=='NO' and authority['C7_EXACT']=='YES'
          (evidence/'authority.json').write_text(json.dumps(authority,sort_keys=True,separators=(',',':'))+'\n',encoding='utf-8')
          print('AUTHORITY_JSON_WRITE=PASS')
          print('C7_AUTHORITY_ROUTE_KIND='+c7_route_kind)
          print('C7_AUTHORITY_ORIGINAL_BASE_PARENT='+c7_original_base_parent)
          print('C7_AUTHORITY_IMMEDIATE_PARENT='+immediate_parent)
          PY
          chmod 0755 "$C7_INPUT_ROOT"
          chmod 0644 "$C7_INPUT_ROOT"/*.json
          echo "C7_QUALIFICATION_ROOTS=PASS"
          echo "C7_EXACT_SHA_BINDING_PREPARED=PASS"

      - name: Build F5C C7 exact image
        if: env.C7_EXACT == 'YES'
        shell: bash
        run: |
          set -euo pipefail
          docker build --pull=false \
            --build-arg AIFE_C7_SOURCE_HEAD="$QUALIFIED_CHECKOUT_SHA" \
            --build-arg AIFE_C7_SOURCE_TREE="$QUALIFIED_CHECKOUT_TREE" \
            -f AIFE/staging/server/Dockerfile.c7 \
            -t "$C7_IMAGE_REF" \
            .
          C7_IMAGE_DIGEST="$(docker image inspect "$C7_IMAGE_REF" --format '{{.Id}}')"
          test -n "$C7_IMAGE_DIGEST"
          echo "C7_IMAGE_DIGEST=$C7_IMAGE_DIGEST" >> "$GITHUB_ENV"
          export C7_IMAGE_DIGEST
          docker image inspect "$C7_IMAGE_DIGEST" > "$C7_EVIDENCE_ROOT/image.json"
          python - <<'PY'
          import json, os
          from pathlib import Path
          image=json.loads((Path(os.environ['C7_EVIDENCE_ROOT'])/'image.json').read_text())[0]
          cfg=image['Config']
          assert cfg['User']=='10001:10001'
          assert cfg['Entrypoint']==['python','-m','server.runtime.c7_carrier']
          assert cfg['Cmd']==['serve']
          labels=cfg['Labels']
          assert labels['org.opencontainers.image.revision']==os.environ['QUALIFIED_CHECKOUT_SHA']
          assert labels['io.aife.f5c.source-tree']==os.environ['QUALIFIED_CHECKOUT_TREE']
          assert labels['io.aife.f5c.qualification-profile']=='C7'
          assert image['Id']==os.environ['C7_IMAGE_DIGEST']
          PY
          echo "C7_IMAGE_BUILD=PASS"
          echo "C7_IMAGE_IDENTITY=PASS"

      - name: Qualify F5C C7 Container A durable acceptance
        if: env.C7_EXACT == 'YES'
        shell: bash
        run: |
          set -euo pipefail
          cleanup_a() { docker rm -f "$C7_CONTAINER_A" >/dev/null 2>&1 || true; }
          trap cleanup_a ERR
          docker run -d --name "$C7_CONTAINER_A" \
            --network none --read-only --tmpfs /tmp \
            -v "$C7_STATE_ROOT/data:/var/lib/aife/data" \
            -v "$C7_STATE_ROOT/control:/var/lib/aife/control" \
            -v "$C7_INPUT_ROOT:/qualification-inputs:ro" \
            "$C7_IMAGE_DIGEST" >/dev/null
          C7_CONTAINER_A_ID="$(docker inspect "$C7_CONTAINER_A" --format '{{.Id}}')"
          test -n "$C7_CONTAINER_A_ID"
          echo "C7_CONTAINER_A_ID=$C7_CONTAINER_A_ID" >> "$GITHUB_ENV"
          docker inspect "$C7_CONTAINER_A" > "$C7_EVIDENCE_ROOT/container-a.json"
          docker exec "$C7_CONTAINER_A" python -m server.runtime.c7_carrier readiness > "$C7_TEMP_ROOT/readiness-a.json"
          docker exec "$C7_CONTAINER_A" python -m server.runtime.c7_carrier run-once \
            --input /qualification-inputs/source-a.json \
            --policy-revision "c7:${QUALIFIED_CHECKOUT_SHA}" \
            --scheduling-slot C7-SOURCE-A \
            --at 2026-09-09T00:01:00Z > "$C7_EVIDENCE_ROOT/source-a-first.json"
          C7_SOURCE_A_WORK_ID="$(python -c 'import json,sys; print(json.load(open(sys.argv[1]))["work_id"])' "$C7_EVIDENCE_ROOT/source-a-first.json")"
          echo "C7_SOURCE_A_WORK_ID=$C7_SOURCE_A_WORK_ID" >> "$GITHUB_ENV"
          docker exec "$C7_CONTAINER_A" python -m server.runtime.c7_carrier readback \
            --work-id "$C7_SOURCE_A_WORK_ID" \
            --expected-content-identity 0f9f5ce47831e099e77e295ed8bb627f089efa8672ee6fbdc49eac6f0d7f5275 \
            > "$C7_EVIDENCE_ROOT/source-a-readback-a.json"
          docker logs "$C7_CONTAINER_A" > "$C7_EVIDENCE_ROOT/container-a.log" 2>&1
          export C7_SOURCE_A_WORK_ID
          python - <<'PY'
          import json, os
          from pathlib import Path
          e=Path(os.environ['C7_EVIDENCE_ROOT'])
          first=json.loads((e/'source-a-first.json').read_text())
          readback=json.loads((e/'source-a-readback-a.json').read_text())
          assert first['status']==readback['status']=='PASS'
          assert first['content_identity']=='0f9f5ce47831e099e77e295ed8bb627f089efa8672ee6fbdc49eac6f0d7f5275'
          assert first['payload_base64']==readback['payload_base64']=='c291cmNlLWE='
          assert first['work_id']==readback['work_id']==os.environ['C7_SOURCE_A_WORK_ID']
          assert first['object_digest']==readback['object_digest']==first['content_identity']
          assert first['object_locator']==readback['object_locator']
          PY
          docker exec -i "$C7_CONTAINER_A" python - <<'PY' > "$C7_EVIDENCE_ROOT/state-before-restart.json"
          import hashlib,json,sqlite3
          from pathlib import Path
          data=Path('/var/lib/aife/data/objects')
          object_rows=[]
          for path in sorted(p for p in data.rglob('*') if p.is_file()):
              object_rows.append((path.relative_to(data).as_posix(),path.stat().st_size,hashlib.sha256(path.read_bytes()).hexdigest()))
          db=Path('/var/lib/aife/control/aife-control.sqlite3')
          with sqlite3.connect(db) as con:
              columns=[row[1] for row in con.execute('PRAGMA table_info(work)').fetchall()]
              expected=['work_id','logical_input_identity','payload_reference','provenance_reference','policy_revision_identity','immutable_input_digest','state']
              assert all(name in columns for name in expected),columns
              works=con.execute('SELECT work_id,logical_input_identity,payload_reference,provenance_reference,policy_revision_identity,immutable_input_digest,state FROM work ORDER BY work_id').fetchall()
          assert len(works)==1
          digest=lambda value: hashlib.sha256(json.dumps(value,sort_keys=True,separators=(',',':')).encode()).hexdigest()
          state={'object_set_sha256':digest(object_rows),'work_set_sha256':digest(works),'object_count':len(object_rows),'work_count':len(works)}
          print(json.dumps(state,sort_keys=True,separators=(',',':')))
          PY
          docker stop -t 10 "$C7_CONTAINER_A" >/dev/null
          docker rm "$C7_CONTAINER_A" >/dev/null
          if docker inspect "$C7_CONTAINER_A" >/dev/null 2>&1; then exit 1; fi
          echo "C7_CONTAINER_A_REMOVED=PASS"
          trap - ERR

      - name: Qualify F5C C7 Container B restart replay and Source B
        if: env.C7_EXACT == 'YES'
        shell: bash
        run: |
          set -euo pipefail
          cleanup_b() { docker rm -f "$C7_CONTAINER_B" >/dev/null 2>&1 || true; }
          docker run -d --name "$C7_CONTAINER_B" \
            --network none --read-only --tmpfs /tmp \
            -v "$C7_STATE_ROOT/data:/var/lib/aife/data" \
            -v "$C7_STATE_ROOT/control:/var/lib/aife/control" \
            -v "$C7_INPUT_ROOT:/qualification-inputs:ro" \
            "$C7_IMAGE_DIGEST" >/dev/null
          C7_CONTAINER_B_ID="$(docker inspect "$C7_CONTAINER_B" --format '{{.Id}}')"
          test -n "$C7_CONTAINER_B_ID"
          test "$C7_CONTAINER_B_ID" != "$C7_CONTAINER_A_ID"
          echo "C7_CONTAINER_B_ID=$C7_CONTAINER_B_ID" >> "$GITHUB_ENV"
          docker inspect "$C7_CONTAINER_B" > "$C7_EVIDENCE_ROOT/container-b.json"
          docker exec "$C7_CONTAINER_B" python -m server.runtime.c7_carrier readiness > "$C7_TEMP_ROOT/readiness-b.json"
          docker exec "$C7_CONTAINER_B" python -m server.runtime.c7_carrier readback \
            --work-id "$C7_SOURCE_A_WORK_ID" \
            --expected-content-identity 0f9f5ce47831e099e77e295ed8bb627f089efa8672ee6fbdc49eac6f0d7f5275 \
            > "$C7_EVIDENCE_ROOT/source-a-readback-b.json"
          docker exec -i "$C7_CONTAINER_B" python - <<'PY' > "$C7_EVIDENCE_ROOT/state-before-replay.json"
          import hashlib,json,sqlite3
          from pathlib import Path
          data=Path('/var/lib/aife/data/objects'); rows=[]
          for p in sorted(x for x in data.rglob('*') if x.is_file()): rows.append((p.relative_to(data).as_posix(),p.stat().st_size,hashlib.sha256(p.read_bytes()).hexdigest()))
          with sqlite3.connect('/var/lib/aife/control/aife-control.sqlite3') as con:
              works=con.execute('SELECT work_id,logical_input_identity,payload_reference,provenance_reference,policy_revision_identity,immutable_input_digest,state FROM work ORDER BY work_id').fetchall()
          digest=lambda v: hashlib.sha256(json.dumps(v,sort_keys=True,separators=(',',':')).encode()).hexdigest()
          pre={'object_set_sha256':digest(rows),'work_set_sha256':digest(works),'object_count':len(rows),'work_count':len(works)}
          print(json.dumps(pre,sort_keys=True,separators=(',',':')))
          PY
          python - <<'PY'
          import json,os
          from pathlib import Path
          e=Path(os.environ['C7_EVIDENCE_ROOT'])
          before=json.loads((e/'state-before-restart.json').read_text())
          pre=json.loads((e/'state-before-replay.json').read_text())
          assert pre==before,(pre,before)
          PY
          docker exec "$C7_CONTAINER_B" python -m server.runtime.c7_carrier run-once \
            --input /qualification-inputs/source-a.json \
            --policy-revision "c7:${QUALIFIED_CHECKOUT_SHA}" \
            --scheduling-slot C7-SOURCE-A \
            --at 2026-09-09T00:02:00Z > "$C7_EVIDENCE_ROOT/source-a-replay.json"
          docker exec "$C7_CONTAINER_B" python -m server.runtime.c7_carrier run-once \
            --input /qualification-inputs/source-b.json \
            --policy-revision "c7:${QUALIFIED_CHECKOUT_SHA}" \
            --scheduling-slot C7-SOURCE-B \
            --at 2026-09-09T00:03:00Z > "$C7_EVIDENCE_ROOT/source-b.json"
          C7_SOURCE_B_WORK_ID="$(python -c 'import json,sys; print(json.load(open(sys.argv[1]))["work_id"])' "$C7_EVIDENCE_ROOT/source-b.json")"
          echo "C7_SOURCE_B_WORK_ID=$C7_SOURCE_B_WORK_ID" >> "$GITHUB_ENV"
          docker exec "$C7_CONTAINER_B" python -m server.runtime.c7_carrier readback \
            --work-id "$C7_SOURCE_B_WORK_ID" \
            --expected-content-identity 3fed13457ee26a4f5b27c42544aa57045981a075a6103aab87e0b81c032e9d01 \
            > "$C7_EVIDENCE_ROOT/source-b-readback.json"
          docker logs "$C7_CONTAINER_B" > "$C7_EVIDENCE_ROOT/container-b.log" 2>&1
          export C7_SOURCE_B_WORK_ID
          python - <<'PY'
          import hashlib,json,os,sqlite3
          from pathlib import Path
          e=Path(os.environ['C7_EVIDENCE_ROOT'])
          first=json.loads((e/'source-a-first.json').read_text()); a2=json.loads((e/'source-a-readback-b.json').read_text()); replay=json.loads((e/'source-a-replay.json').read_text())
          b=json.loads((e/'source-b.json').read_text()); br=json.loads((e/'source-b-readback.json').read_text())
          assert a2['status']==replay['status']==b['status']==br['status']=='PASS'
          assert first['work_id']==a2['work_id']==replay['work_id']==os.environ['C7_SOURCE_A_WORK_ID']
          assert first['object_digest']==a2['object_digest']==replay['object_digest']
          assert first['object_locator']==a2['object_locator']==replay['object_locator']
          assert b['work_id']==br['work_id']==os.environ['C7_SOURCE_B_WORK_ID']
          assert b['content_identity']==br['content_identity']=='3fed13457ee26a4f5b27c42544aa57045981a075a6103aab87e0b81c032e9d01'
          assert b['payload_base64']==br['payload_base64']=='c291cmNlLWI='
          PY
          docker exec -i "$C7_CONTAINER_B" python - "$C7_SOURCE_A_WORK_ID" <<'PY' > "$C7_EVIDENCE_ROOT/state-after-restart.json"
          import hashlib,json,sqlite3,sys
          from pathlib import Path
          source_a_work_id=sys.argv[1]
          data=Path('/var/lib/aife/data/objects'); rows=[]
          for p in sorted(x for x in data.rglob('*') if x.is_file()): rows.append((p.relative_to(data).as_posix(),p.stat().st_size,hashlib.sha256(p.read_bytes()).hexdigest()))
          with sqlite3.connect('/var/lib/aife/control/aife-control.sqlite3') as con:
              source_a_count=con.execute('SELECT COUNT(*) FROM work WHERE work_id=?',(source_a_work_id,)).fetchone()[0]
              works=con.execute('SELECT work_id,logical_input_identity,payload_reference,provenance_reference,policy_revision_identity,immutable_input_digest,state FROM work ORDER BY work_id').fetchall()
          assert source_a_count==1 and len(works)==2
          digest=lambda v: hashlib.sha256(json.dumps(v,sort_keys=True,separators=(',',':')).encode()).hexdigest()
          final={'object_set_sha256':digest(rows),'work_set_sha256':digest(works),'object_count':len(rows),'work_count':len(works)}
          print(json.dumps({'final':final},sort_keys=True,separators=(',',':')))
          PY
          echo "PHYSICAL_RESTART=PASS"
          echo "SOURCE_A_REPLAY=PASS"
          echo "SOURCE_B_GENERICITY=PASS"

      - name: Execute F5C C7 NC01-NC11 negative controls
        if: env.C7_EXACT == 'YES'
        shell: bash
        run: |
          set -euo pipefail
          export C7_SOURCE_B_WORK_ID
          docker exec -i "$C7_CONTAINER_B" python - <<'PY'
          import hashlib
          from pathlib import Path
          from server.storage.filesystem import QualifiedDataRootImmutableFilesystem
          from server.storage.ports import ImmutableObjectConflict
          root=Path('/var/lib/aife/data/nc01')
          store=QualifiedDataRootImmutableFilesystem(root)
          payload=b'nc01-corrupt-object'
          digest=hashlib.sha256(payload).hexdigest()
          evidence=store.write_immutable(payload,expected_digest=digest)
          target=root/evidence.physical_locator
          target.write_bytes(b'corrupted')
          try:
              store.readback_verify(digest,expected_size=len(payload))
          except ImmutableObjectConflict:
              print('NC01=PASS')
          else:
              raise AssertionError('NC01 corrupt object was accepted')
          PY

          docker exec -i "$C7_CONTAINER_B" python - <<'PY'
          import sqlite3
          from datetime import datetime,timezone
          from pathlib import Path
          from core.data.adapters.sqlite_control import SQLiteServerControlRepository
          from server.acquisition.service import DurableAcquisitionAcceptance
          from server.runtime.c7_carrier import _load_input
          from server.storage.filesystem import QualifiedDataRootImmutableFilesystem
          class FailingRepository(SQLiteServerControlRepository):
              def accept_work(self,*args,**kwargs):
                  raise sqlite3.OperationalError('injected Work INSERT failure')
          db=Path('/var/lib/aife/control/nc02.sqlite3')
          repo=FailingRepository(db)
          artifact=_load_input(Path('/qualification-inputs/source-a.json'))
          store=QualifiedDataRootImmutableFilesystem('/var/lib/aife/data/nc02')
          acceptance=DurableAcquisitionAcceptance(store,repo,'c7:nc02','C7-NC02')
          try:
              acceptance.accept(artifact,at=datetime(2026,9,9,tzinfo=timezone.utc))
          except sqlite3.OperationalError:
              pass
          else:
              raise AssertionError('NC02 injected Work failure did not fail')
          observed=store.read_exact(artifact.envelope.content_identity)
          assert observed==artifact.payload
          with sqlite3.connect(db) as con:
              assert con.execute('SELECT COUNT(*) FROM work').fetchone()[0]==0
          print('NC02=PASS')
          PY

          docker exec "$C7_CONTAINER_B" python -m server.runtime.c7_carrier \
            --data-root /var/lib/aife/data/nc03 \
            --control-db /var/lib/aife/control/nc03.sqlite3 \
            run-once --input /qualification-inputs/source-a.json \
            --policy-revision c7:nc03 --scheduling-slot C7-NC03 \
            --at 2026-09-09T00:04:00Z > "$C7_TEMP_ROOT/nc03-create.json"
          NC03_WORK_ID="$(python -c 'import json,sys; print(json.load(open(sys.argv[1]))["work_id"])' "$C7_TEMP_ROOT/nc03-create.json")"
          docker exec "$C7_CONTAINER_B" rm -f /var/lib/aife/data/nc03/objects/sha256/0f/0f9f5ce47831e099e77e295ed8bb627f089efa8672ee6fbdc49eac6f0d7f5275
          if docker exec "$C7_CONTAINER_B" python -m server.runtime.c7_carrier \
              --data-root /var/lib/aife/data/nc03 \
              --control-db /var/lib/aife/control/nc03.sqlite3 \
              readback --work-id "$NC03_WORK_ID" \
              --expected-content-identity 0f9f5ce47831e099e77e295ed8bb627f089efa8672ee6fbdc49eac6f0d7f5275 \
              > "$C7_TEMP_ROOT/nc03-readback.json" 2> "$C7_TEMP_ROOT/nc03.err"; then
            echo 'NC03 unexpectedly passed' >&2; exit 1
          fi
          grep -q 'status":"FAIL' "$C7_TEMP_ROOT/nc03-readback.json"
          echo 'NC03=PASS'

          if docker exec "$C7_CONTAINER_B" python -m server.runtime.c7_carrier \
              --data-root /var/lib/aife/data/nc04 \
              --control-db /var/lib/aife/control/nc04.sqlite3 \
              readback --work-id work-does-not-exist \
              --expected-content-identity 0f9f5ce47831e099e77e295ed8bb627f089efa8672ee6fbdc49eac6f0d7f5275 \
              > "$C7_TEMP_ROOT/nc04.json" 2> "$C7_TEMP_ROOT/nc04.err"; then
            echo 'NC04 unexpectedly passed' >&2; exit 1
          fi
          grep -q 'requested Work does not exist' "$C7_TEMP_ROOT/nc04.json"
          echo 'NC04=PASS'

          docker exec -i "$C7_CONTAINER_B" python - <<'PY'
          import sqlite3
          from datetime import datetime,timezone
          from core.data.adapters.sqlite_control import SQLiteServerControlRepository
          from core.data.repositories.server_control import WorkIdentityConflict
          from server.work.models import F5WorkIdentityInputs
          db='/var/lib/aife/control/nc05.sqlite3'
          repo=SQLiteServerControlRepository(db)
          inputs=F5WorkIdentityInputs('nc05-artifact','nc05-revision','a'*64,'c7:nc05','C7-NC05')
          repo.accept_work(inputs,payload_reference='objects/first',provenance_reference='nc05',created_at=datetime(2026,9,9,tzinfo=timezone.utc))
          try:
              repo.accept_work(inputs,payload_reference='objects/conflict',provenance_reference='nc05',created_at=datetime(2026,9,9,tzinfo=timezone.utc))
          except WorkIdentityConflict:
              pass
          else:
              raise AssertionError('NC05 duplicate/conflicting Work was accepted')
          with sqlite3.connect(db) as con:
              assert con.execute('SELECT COUNT(*) FROM work').fetchone()[0]==1
          print('NC05=PASS')
          PY

          python - "$C7_INPUT_ROOT/source-a.json" "$C7_INPUT_ROOT/nc06-bad-digest.json" <<'PY'
          import json,sys
          value=json.load(open(sys.argv[1]))
          value['content_identity_sha256']='0'*64
          with open(sys.argv[2],'w') as f: json.dump(value,f,sort_keys=True,separators=(',',':'))
          PY
          if docker exec "$C7_CONTAINER_B" python -m server.runtime.c7_carrier run-once \
              --input /qualification-inputs/nc06-bad-digest.json \
              --policy-revision c7:nc06 --scheduling-slot C7-NC06 \
              --at 2026-09-09T00:05:00Z > "$C7_TEMP_ROOT/nc06.json" 2> "$C7_TEMP_ROOT/nc06.err"; then
            echo 'NC06 unexpectedly passed' >&2; exit 1
          fi
          grep -q 'decoded payload SHA-256 differs' "$C7_TEMP_ROOT/nc06.json"
          echo 'NC06=PASS'

          if docker exec "$C7_CONTAINER_B" grep -Eiq 'binance|kraken|deribit|provider[_ -]?specific' /app/server/runtime/c7_carrier.py; then
            echo 'NC07 provider-specific carrier content detected' >&2; exit 1
          fi
          echo 'NC07=PASS'

          ACTUAL_REVISION="$(docker image inspect "$C7_IMAGE_DIGEST" --format '{{index .Config.Labels "org.opencontainers.image.revision"}}')"
          check_image_revision() { [[ "$ACTUAL_REVISION" == "$1" ]]; }
          check_image_revision "$QUALIFIED_CHECKOUT_SHA"
          if check_image_revision "0000000000000000000000000000000000000000"; then
            echo 'NC08 wrong expected image revision was accepted' >&2; exit 1
          fi
          echo 'NC08=PASS'

          python - <<'PY'
          import json,os
          from pathlib import Path
          c=json.loads((Path(os.environ['C7_EVIDENCE_ROOT'])/'container-b.json').read_text())[0]
          destinations=sorted(m['Destination'] for m in c['Mounts'])
          assert destinations==['/qualification-inputs','/var/lib/aife/control','/var/lib/aife/data'],destinations
          assert c['HostConfig']['NetworkMode']=='none'
          assert c['HostConfig']['ReadonlyRootfs'] is True
          assert '/tmp' in c['HostConfig']['Tmpfs']
          print('NC09=PASS')
          image=json.loads((Path(os.environ['C7_EVIDENCE_ROOT'])/'image.json').read_text())[0]
          cfg=image['Config']
          assert cfg['Entrypoint']==['python','-m','server.runtime.c7_carrier']
          assert cfg['Cmd']==['serve']
          print('NC10=PASS')
          PY

          expected="$(printf '%s\n' \
            $'M\t.github/workflows/qualify-d8-runtime.yml' \
            $'A\tAIFE/staging/server/Dockerfile.c7' \
            $'A\tAIFE/staging/server/runtime/c7_carrier.py' | sort)"
          actual="$(git diff --name-status b645d34a992c0fc88d8cb09aed6fa6913c5190ad HEAD | sort)"
          test "$actual" = "$expected"
          bad_actual="$(printf '%s\n%s\n' "$actual" $'A\tAIFE/staging/server/forbidden-fourth-path' | sort)"
          test "$bad_actual" != "$expected"
          echo 'NC11=PASS'

          python - <<'PY'
          import json,os
          from pathlib import Path
          value={f'NC{i:02d}':'PASS' for i in range(1,12)}
          (Path(os.environ['C7_EVIDENCE_ROOT'])/'negative-controls.json').write_text(json.dumps(value,sort_keys=True,separators=(',',':'))+'\n')
          PY
          echo 'NC01_NC11=11/11_PASS'

      - name: Verify F5C C7 P01-P20 proof matrix and evidence
        if: env.C7_EXACT == 'YES'
        shell: bash
        run: |
          set -euo pipefail
          export C7_CONTAINER_A_ID C7_CONTAINER_B_ID C7_SOURCE_A_WORK_ID C7_SOURCE_B_WORK_ID C7_IMAGE_DIGEST
          python - <<'PY'
          import json,os,subprocess
          from pathlib import Path
          e=Path(os.environ['C7_EVIDENCE_ROOT'])
          authority=json.loads((e/'authority.json').read_text())
          image=json.loads((e/'image.json').read_text())[0]
          a1=json.loads((e/'source-a-first.json').read_text())
          ar1=json.loads((e/'source-a-readback-a.json').read_text())
          ar2=json.loads((e/'source-a-readback-b.json').read_text())
          replay=json.loads((e/'source-a-replay.json').read_text())
          b=json.loads((e/'source-b.json').read_text())
          br=json.loads((e/'source-b-readback.json').read_text())
          before=json.loads((e/'state-before-restart.json').read_text())
          pre_replay=json.loads((e/'state-before-replay.json').read_text())
          after=json.loads((e/'state-after-restart.json').read_text())
          neg=json.loads((e/'negative-controls.json').read_text())
          cfg=image['Config']; labels=cfg['Labels']
          proofs={
            'P01': authority['qualified_checkout_sha']==os.environ['QUALIFIED_CHECKOUT_SHA'],
            'P02': authority['qualified_checkout_tree']==os.environ['QUALIFIED_CHECKOUT_TREE'],
            'P03': authority['c7_original_base_parent']=='b645d34a992c0fc88d8cb09aed6fa6913c5190ad' and authority['c7_route_kind'] in {'INITIAL_IMPLEMENTATION','AUTHORIZED_REPAIR_SUCCESSOR','AUTHORIZED_C7_AUTHORITY_REPAIR_SUCCESSOR','AUTHORIZED_C7_STATE_ACCESS_REPAIR_SUCCESSOR'} and authority['immediate_parent']=={'INITIAL_IMPLEMENTATION':'b645d34a992c0fc88d8cb09aed6fa6913c5190ad','AUTHORIZED_REPAIR_SUCCESSOR':'273653636de5190dcb80324148ddcdbe0a66b2a7','AUTHORIZED_C7_AUTHORITY_REPAIR_SUCCESSOR':'7817ac772ee3d4278c9e4dff347ce3936e523c84','AUTHORIZED_C7_STATE_ACCESS_REPAIR_SUCCESSOR':'9e8eb8d5b8e8a708e58a17da0d771dbe1b473a8c'}[authority['c7_route_kind']],
            'P04': authority['C6_EXACT']=='NO' and authority['C7_EXACT']=='YES',
            'P05': image['Id']==os.environ['C7_IMAGE_DIGEST'],
            'P06': labels['org.opencontainers.image.revision']==os.environ['QUALIFIED_CHECKOUT_SHA'],
            'P07': labels['io.aife.f5c.source-tree']==os.environ['QUALIFIED_CHECKOUT_TREE'],
            'P08': labels['io.aife.f5c.qualification-profile']=='C7',
            'P09': cfg['User']=='10001:10001',
            'P10': cfg['Entrypoint']==['python','-m','server.runtime.c7_carrier'] and cfg['Cmd']==['serve'],
            'P11': json.load(open(Path(os.environ['C7_TEMP_ROOT'])/'readiness-a.json'))['status']=='PASS',
            'P12': a1['status']=='PASS' and a1['content_identity']=='0f9f5ce47831e099e77e295ed8bb627f089efa8672ee6fbdc49eac6f0d7f5275',
            'P13': ar1['work_id']==a1['work_id'] and ar1['payload_base64']=='c291cmNlLWE=',
            'P14': before['work_count']==1 and before['object_count']==1,
            'P15': os.environ['C7_CONTAINER_A_ID']!=os.environ['C7_CONTAINER_B_ID'],
            'P16': json.load(open(Path(os.environ['C7_TEMP_ROOT'])/'readiness-b.json'))['status']=='PASS',
            'P17': pre_replay==before,
            'P18': replay['work_id']==a1['work_id']==ar2['work_id'] and replay['object_digest']==a1['object_digest'],
            'P19': b['status']=='PASS' and br['work_id']==b['work_id'] and b['payload_base64']==br['payload_base64']=='c291cmNlLWI=',
            'P20': after['final']['work_count']==2 and after['final']['object_count']==2 and all(v=='PASS' for v in neg.values()),
          }
          failed=[k for k,v in proofs.items() if not v]
          assert not failed,failed
          (e/'positive-proofs.json').write_text(json.dumps({k:'PASS' for k in proofs},sort_keys=True,separators=(',',':'))+'\n')
          required=['authority.json','image.json','source-a-first.json','source-a-readback-a.json','container-a.json','state-before-restart.json','state-before-replay.json','container-b.json','source-a-readback-b.json','source-a-replay.json','source-b.json','state-after-restart.json','negative-controls.json','positive-proofs.json','source-b-readback.json','container-a.log','container-b.log']
          for name in required: assert (e/name).is_file(),name
          print('P01_P20=20/20_PASS')
          print('NC01_NC11=11/11_PASS')
          print('PHYSICAL_RESTART=PASS')
          print('SOURCE_A_REPLAY=PASS')
          print('SOURCE_B_GENERICITY=PASS')
          print('NO_NEW_STORAGE_AUTHORITY=PASS')
          print('C7_EXACT_SHA_BINDING=PASS')
          PY
          (cd "$C7_EVIDENCE_ROOT" && sha256sum $(find . -maxdepth 1 -type f ! -name sha256sums.txt -printf '%f\n' | sort) > sha256sums.txt)
          (cd "$C7_EVIDENCE_ROOT" && sha256sum -c sha256sums.txt)
          echo 'SHA256SUMS=PASS'

      - name: Upload F5C C7 qualification evidence
        if: always() && env.C7_EXACT == 'YES'
        uses: actions/upload-artifact@v4
        with:
          name: f5c-c7-${{ github.sha }}-${{ github.run_attempt }}
          path: ${{ env.C7_EVIDENCE_ROOT }}
          if-no-files-found: error
          retention-days: 30

      - name: Cleanup F5C C7 containers
        if: always() && env.C7_EXACT == 'YES'
        shell: bash
        run: |
          docker rm -f "$C7_CONTAINER_A" "$C7_CONTAINER_B" >/dev/null 2>&1 || true
          echo 'C7_CONTAINER_CLEANUP=PASS'

      - name: Build D8 OCI-compatible image
        if: env.C6_SOURCE_QUALIFICATION != 'YES' && env.C7_EXACT != 'YES' && env.C9_SOURCE_REPAIR_QUALIFICATION != 'YES'
        shell: bash
        run: |
          set -euo pipefail
          IMAGE="eth-macro-d8:${QUALIFIED_CHECKOUT_SHA}"
          docker build -f tools/d8/Dockerfile -t "$IMAGE" .
          DIGEST="$(docker image inspect "$IMAGE" --format '{{.Id}}')"
          test -n "$DIGEST"
          printf 'QUALIFIED_CONTAINER_IMAGE_REF=%s\n' "$IMAGE" >> "$GITHUB_ENV"
          printf 'QUALIFIED_CONTAINER_IMAGE_DIGEST=%s\n' "$DIGEST" >> "$GITHUB_ENV"
          echo "CONTAINER_BUILD=PASS"
          echo "CONTAINER_IMAGE_REF=$IMAGE"
          echo "QUALIFIED_CONTAINER_IMAGE_REF=$IMAGE"
          echo "QUALIFIED_CONTAINER_IMAGE_DIGEST=$DIGEST"
          uid=$(docker run --rm --entrypoint id "$IMAGE" -u)
          test "$uid" = "10001"
          echo "CONTAINER_NON_ROOT=PASS"

      - name: Qualify persistent container restart and same-slot replay
        if: env.C6_SOURCE_QUALIFICATION != 'YES' && env.C7_EXACT != 'YES' && env.C9_SOURCE_REPAIR_QUALIFICATION != 'YES'
        shell: bash
        run: |
          set -euo pipefail
          TOKEN='d8-ci-fixture-token'
          VOLUME='d8-ci-state'
          IMAGE="$QUALIFIED_CONTAINER_IMAGE_REF"
          ACTUAL_IMAGE_DIGEST="$(docker image inspect "$IMAGE" --format '{{.Id}}')"
          test "$ACTUAL_IMAGE_DIGEST" = "$QUALIFIED_CONTAINER_IMAGE_DIGEST"
          cleanup() { docker rm -f d8-ci-1 d8-ci-2 >/dev/null 2>&1 || true; docker volume rm "$VOLUME" >/dev/null 2>&1 || true; }
          trap cleanup EXIT
          docker volume create "$VOLUME" >/dev/null
          start_container() {
            local name="$1"
            docker run -d --name "$name" -p 127.0.0.1:18080:8080 \
              -v "$VOLUME":/var/lib/eth-macro-data-bridge \
              -e D8_RUNTIME_PROFILE=test \
              -e D8_PROVIDER_MODE=mock \
              -e D8_RUNTIME_TOKEN="$TOKEN" \
              -e D8_SOURCE_REVISION="$QUALIFIED_CHECKOUT_SHA" \
              "$IMAGE" >/dev/null
            for _ in $(seq 1 60); do
              if curl -fsS -H "Authorization: Bearer $TOKEN" http://127.0.0.1:18080/v1/readiness > readiness.json; then return 0; fi
              sleep 1
            done
            docker logs "$name"
            return 1
          }
          start_container d8-ci-1
          runtime_source="$(docker exec d8-ci-1 python -c 'from d8_runtime import config_from_env; print(config_from_env().source_revision)')"
          test "$runtime_source" = "$QUALIFIED_CHECKOUT_SHA"
          echo "D8_SOURCE_REVISION_USED=$runtime_source"
          echo "CONTAINER_SOURCE_BINDING=PASS"
          slot=$(python - <<'PY'
          from datetime import datetime,timezone
          d=datetime.now(timezone.utc).replace(second=0,microsecond=0)
          d=d.replace(minute=(d.minute//5)*5)
          print(d.isoformat().replace('+00:00','Z'))
          PY
          )
          python - "$slot" > request.json <<'PY'
          import json,sys
          print(json.dumps({'schema_version':'eth-macro-d8-collect-cycle-request/1.0.0','expected_schedule_at':sys.argv[1],'canonical_slot':'M5','trace_id':'ci'}))
          PY
          curl -fsS -H "Authorization: Bearer $TOKEN" -H 'Content-Type: application/json' --data @request.json http://127.0.0.1:18080/v1/collect-cycle > first.json
          python - <<'PY'
          import json
          r=json.load(open('first.json'))
          assert r['overall_status']=='PASS', r
          assert r['hot_promotion']=='PROMOTED', r
          assert r['collection_gap_summary']['synthetic_fill'] is False
          print('FIRST_CYCLE_ID='+r['cycle_id'])
          PY
          echo "CONTAINER_MEMORY_FIRST=$(docker stats --no-stream --format '{{.MemUsage}}' d8-ci-1)"
          first_id=$(python -c "import json; print(json.load(open('first.json'))['cycle_id'])")
          persisted_source="$(docker exec d8-ci-1 python -c 'import sqlite3,sys; db=sqlite3.connect("/var/lib/eth-macro-data-bridge/d8-runtime.sqlite3"); row=db.execute("SELECT source_revision FROM cycles WHERE cycle_id=?",(sys.argv[1],)).fetchone(); print(row[0] if row else "")' "$first_id")"
          test "$persisted_source" = "$QUALIFIED_CHECKOUT_SHA"
          echo "D8_PERSISTED_CYCLE_SOURCE_REVISION=$persisted_source"
          echo "D8_PERSISTED_SOURCE_BINDING=PASS"
          docker stop -t 10 d8-ci-1 >/dev/null
          docker rm d8-ci-1 >/dev/null
          start_container d8-ci-2
          restart_source="$(docker exec d8-ci-2 python -c 'from d8_runtime import config_from_env; print(config_from_env().source_revision)')"
          test "$restart_source" = "$QUALIFIED_CHECKOUT_SHA"
          curl -fsS -H "Authorization: Bearer $TOKEN" -H 'Content-Type: application/json' --data @request.json http://127.0.0.1:18080/v1/collect-cycle > replay.json
          curl -fsS -H "Authorization: Bearer $TOKEN" http://127.0.0.1:18080/v1/readiness > readiness-after.json
          python - "$first_id" <<'PY'
          import json,sys
          replay=json.load(open('replay.json')); ready=json.load(open('readiness-after.json'))
          assert replay['cycle_id']==sys.argv[1]
          assert replay['overall_status']=='PASS'
          assert replay['replayed'] is True
          assert ready['hot_cycle_id']==sys.argv[1]
          assert ready['spool_rows'] > 0
          assert ready['ledger_rows'] > 0
          print('CONTAINER_RESTART=PASS')
          print('CONTAINER_SAME_SLOT_REPLAY=PASS')
          print('CONTAINER_HOT_RETAINED=PASS')
          print('CONTAINER_SPOOL_RETAINED=PASS')
          print('CONTAINER_LEDGER_RETAINED=PASS')
          PY
          echo "CONTAINER_IMAGE_REF=$IMAGE"
          echo "QUALIFIED_CONTAINER_IMAGE_DIGEST=$QUALIFIED_CONTAINER_IMAGE_DIGEST"
          echo "CONTAINER_SOURCE_BINDING=PASS"
          echo "CONTAINER_MEMORY_RESTART=$(docker stats --no-stream --format '{{.MemUsage}}' d8-ci-2)"
          echo "CONTAINER_RUNTIME_TEST=PASS"
